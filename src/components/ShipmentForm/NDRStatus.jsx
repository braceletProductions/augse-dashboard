import React from "react";
import CardWithBackDrop from "../Settings/CardWithBackDrop";

function NDRStatus({ onClose }) {
  return <CardWithBackDrop onClose={onClose}></CardWithBackDrop>;
}

export default NDRStatus;
